#!/bin/bash
# Deprecated no-op shim — waybar is gone, quickshell needs no bar reload.
# Kept so old keybinds/scripts referencing it don't error.
echo "reload-waybar.sh is deprecated (quickshell needs no bar reload)."
exit 0
